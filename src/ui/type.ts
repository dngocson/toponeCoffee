export interface CarouselProps {
  id: number;
  label: string;
  img: string;
}
export interface MenuItemProps {
  price: number;
  name: string;
  description: string;
  imgURL: string;
  promotion: string;
}
